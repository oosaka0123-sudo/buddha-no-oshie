const b=document.querySelector('.menu-btn'),m=document.querySelector('.menu');if(b&&m){b.addEventListener('click',()=>{const o=b.getAttribute('aria-expanded')==='true';b.setAttribute('aria-expanded',String(!o));b.setAttribute('aria-label',o?'メニューを開く':'メニューを閉じる');m.classList.toggle('open',!o);m.setAttribute('aria-hidden',String(o));document.body.style.overflow=o?'':'hidden'});document.querySelectorAll('.menu a').forEach(a=>a.addEventListener('click',()=>{m.classList.remove('open');document.body.style.overflow=''}))}if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.15});document.querySelectorAll('.reveal').forEach(e=>io.observe(e))}else{document.querySelectorAll('.reveal').forEach(e=>e.classList.add('in'))}